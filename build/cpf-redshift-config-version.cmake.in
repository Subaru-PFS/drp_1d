
set(PACKAGE_VERSION @VERSION@)

if("${PACKAGE_FIND_VERSION}" STREQUAL @VERSION@ )
   set(PACKAGE_VERSION_COMPATIBLE 1)
   set(PACKAGE_VERSION_EXACT 1)
endif("${PACKAGE_FIND_VERSION}" STREQUAL @VERSION@)